{
  "name": "@resume-copilot/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Temporal workflow orchestrator for Resume Copilot",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint \"src/**/*.{ts,tsx}\""
  },
  "dependencies": {
    "temporalio": "^1.9.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
